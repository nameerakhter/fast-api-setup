# Diagram placeholders

Add teaching slides or exported images here. The README links to:

| File | Topic |
| ---- | ----- |
| `script-vs-real-app.png` | Why a one-off script is not enough for a website |
| `real-app-request-flow.png` | React → FastAPI → MongoDB → JSON |
| `react-no-direct-mongodb.png` | Why the browser cannot connect to MongoDB directly |

Until images exist, the README still references these paths so you can drop files in later.
